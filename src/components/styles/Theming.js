/**
 * 
 * https://material-ui.com/customization/default-theme/#default-theme
 * 
 * breakpoints
 *   keys - tipos de tamamho de tela
 *   values - largura da tela em mixel para cada tipo de tamanho
 * 
 * pallete:
 *   definição de todas as cores usados no tema, para cada tipo de componentes, textos, etx
 *   cada definição existe 4 cores: main, dark theme, light theme e high contrast; já preparados
 *    para alterar a aparência do site entre os modos principais
 * 
 * typography
 *     definição dos estilos de fontes (tipo, tamanho, formato, etc) 
 * 
 * 
 * Componente ThemeProvider permite definir / customizar seu próprio Theme
 *   Esse provider deve ser usado para encapsular a tag <App> dentro do index.js,
 *   aplicando assim novo theme a toda a aplicação
 * 
 */